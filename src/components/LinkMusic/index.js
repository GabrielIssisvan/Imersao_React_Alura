import styled from 'styled-components';

const LinkMusic = styled.a`
    font-weight:bold;
    color: var(--color-primary-medium);
    cursor:pointer;
    text-decoration:none;
    font-size:16rem;
`;

export default LinkMusic;